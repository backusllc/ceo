import { style } from '@vanilla-extract/css';

export const ja: string = style({
  borderBottom: '1px dotted #ccc',
  marginBottom: '30px',
})

export const p: string = style({
  textAlign: 'center',
})
